create table teams (
                       id bigint(10) not null auto_increment,
                       name varchar(255),
                       players varchar(255),
                       primary key (id));